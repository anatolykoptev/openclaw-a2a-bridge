{
  "id": "a2a-bridge",
  "name": "A2A Bridge",
  "version": "1.0.0",
  "description": "Standard A2A protocol bridge — serves agent card and JSON-RPC endpoint, provides tools for calling remote A2A agents",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "secret": {
        "type": "string",
        "description": "Bearer token for authenticating inbound A2A requests"
      },
      "agentName": {
        "type": "string",
        "description": "Name advertised in the agent card (default: OpenClaw)"
      },
      "agentDescription": {
        "type": "string",
        "description": "Description advertised in the agent card"
      },
      "remoteAgents": {
        "type": "object",
        "description": "Remote A2A agents keyed by ID",
        "additionalProperties": {
          "type": "object",
          "properties": {
            "url": { "type": "string" },
            "token": { "type": "string" },
            "alias": { "type": "string" }
          },
          "required": ["url"]
        }
      }
    }
  },
  "uiHints": {
    "secret": { "label": "A2A Bearer Secret", "sensitive": true }
  }
}
